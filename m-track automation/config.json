{
    "mtrack_url": "http://172.16.101.50:3000/",
    "google_sheet_id": "https://docs.google.com/spreadsheets/d/1tV5zOxLg533U9YU3dYYcGJKxA7b7xTn2/edit?gid=1102329858#gid=1102329858",
    "sheet_gid": "",
    "_comment_auth": "Sheet is fetched via the automation browser. Set local_csv_path to a CSV to skip Google login entirely.",
    "local_csv_path": "",
    "headless": false,
    "submit_delay_seconds": 1,
    "skip_columns": [
        "_comment: list sheet column headers that have no matching field in the m-track form",
        "REQUEST NO.",
        "REVIEW & CLOSED"
    ],
    "field_mapping": {
        "_comment": "Overrides only — add an entry when the sheet column header differs from the m-track form label.",
        "_comment2": "All other columns are auto-mapped to  label:<column header>  at runtime.",
        "_comment3": "Selector formats: 'label:Label Text'  |  'id:field-id'  |  'name:field-name'  |  'css:.selector'",
        "_example": "\"REQUESTED ACTIVITY\": \"label:Activity Type\""
    },
    "button_selectors": {
        "request_form_button": [
            "//button[contains(text(), 'Request Form')]",
            "//a[contains(text(), 'Request Form')]",
            "//*[contains(@class, 'request-form')]",
            "//*[contains(text(), 'Request Form')]"
        ],
        "new_request_button": [
            "//button[contains(text(), '+New Request')]",
            "//button[contains(text(), 'New Request')]",
            "//a[contains(text(), 'New Request')]",
            "//*[contains(@class, 'new-request')]",
            "//*[contains(text(), 'New Request')]"
        ],
        "submit_button": [
            "//button[contains(text(), 'Submit')]",
            "//button[@type='submit']",
            "//input[@type='submit']",
            "//*[contains(@class, 'submit')]"
        ]
    }
}
